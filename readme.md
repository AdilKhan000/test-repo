It is recommended to restrict the scope of the session cookie by entirely omitting the Domain attribute from the Set-Cookie header. When omitted, the browser defaults to sending the cookie only to the exact origin server that issued it.
